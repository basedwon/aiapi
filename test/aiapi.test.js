const { _, log } = require('basd')

// const aiapi = require('../lib/aiapi')

// describe('AIAPI', () => {
//   it('should work...', () => {
//   })
// })

async function test() {
  log('aiapi')
}

_.executor(test)
